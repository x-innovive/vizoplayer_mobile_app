import 'package:flutter/material.dart';

class ContactUsText extends StatelessWidget {
  const ContactUsText({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      'Contact Us: +44 0000000000',
      textAlign: TextAlign.center,
    );
  }
}
